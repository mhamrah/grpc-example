type: google.api.Service
config_version: 3

name: todos.endpoints.grpc-demo-1.cloud.goog

title: Todos gRPC API
apis:
  - name: todos.Todos

usage:
  rules:
    - selector: "*"
      allow_unregistered_calls: true

